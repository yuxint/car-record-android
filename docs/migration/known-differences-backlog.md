# 已知差异与后续 Backlog（Step 12）

## P1（发布前建议完成）
- [ ] 补齐 Gradle Wrapper（`gradlew`、`gradlew.bat`、`gradle/wrapper/*`），使项目可在 CI 独立执行。
- [ ] 补齐上下文自动化测试：`manual now` 与导航请求单次消费。
- [ ] 在独立仓库配置 Android CI（最小流水线：编译 + 单测）。

## P2（发布后尽快）
- [ ] 为签名配置建立环境变量规范与模板（仅保留占位，不提交密钥）。
- [ ] 增加发布版本号策略文档（`versionCode`/`versionName` 递增规则）。
- [ ] 补齐最小冒烟测试清单（安装、首页切 Tab、记录新增、备份导入导出）。

## P3（持续优化）
- [ ] 将当前 UI 占位页逐步替换为正式交互稿。
- [ ] 增加跨模块回归脚本，覆盖迁移规则的端到端路径。

## 备注
- 本文件对应 Step 12 要求中的“已知差异建立 backlog 与优先级”。
